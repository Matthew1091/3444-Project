<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SongActivity"
    android:background="@color/GreenBB"
    android:padding="15dp">

    <RelativeLayout
        android:id="@+id/topSLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/selectSongImage"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="40dp"
            android:contentDescription="@string/img"
            android:src="@drawable/ic_music_note" />

        <TextView
            android:id="@+id/selectSong_Text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="40dp"
            android:layout_below="@+id/selectSongImage"
            android:text="@string/SongActivityMainText"
            android:fontFamily="sans-serif-light"
            android:textColor="@color/WhiteBB"
            android:textStyle="bold"
            android:textSize="30sp"
            android:textAlignment="center" />

    </RelativeLayout>
    <RelativeLayout
        android:id="@+id/middleSLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/topSLayout"
        android:layout_marginTop="30dp">

        <Button
            android:id="@+id/selectSong"
            style="?android:attr/spinnerStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:paddingLeft="15dp"
            android:drawableLeft="@drawable/ic_library_music_24px"
            android:textColor="@color/WhiteBB"
            android:fontFamily="sans-serif"
            android:textAlignment="center"
            android:textAllCaps="false"
            android:background="@drawable/ic_search"/>

    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/bottomSLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="bottom"
        android:layout_below="@+id/middleSLayout">

        <Button
            android:id="@+id/uploadBtn"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="50dp"
            android:background="@drawable/ic_nextbutton"
            android:fontFamily="sans-serif"
            android:padding="15dp"
            android:text="Continue"
            android:textAllCaps="false"
            android:textColor="@color/WhiteBB"
            android:textSize="25sp"/>


    </RelativeLayout>


</RelativeLayout>